import React from 'react';
import ReactDOM from 'react-dom/client';
import DeepRehearsal from './DeepRehearsal';
import './deep.css';

ReactDOM.createRoot(document.getElementById('deep-root')!).render(
  <React.StrictMode>
    <DeepRehearsal />
  </React.StrictMode>,
);
